"""The MCP server. Primary interface, not an afterthought.

Note for readers of the import graph: this package is ``mcpnews.mcp``; the SDK it
uses is the top-level ``mcp`` package. Python 3 uses absolute imports, so the two
never collide.
"""
