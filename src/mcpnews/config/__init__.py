"""Configuration: settings, profile, providers and dated source bundles.

Written by the dashboard, stored as readable YAML. The GUI is the primary editor;
the files exist so the reader owns, can inspect and can move their configuration.
"""
